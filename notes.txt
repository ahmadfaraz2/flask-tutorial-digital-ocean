~~~~~~~~~~~~~~~~~~~~~Setting Up The Environment(Powershell)~~~~~~~~~~~~~~~~~~~~~~~~

1): Create a Virtual Environment
    >>> python -m pipenv shell
2): Install Flask
    >>> pip install Flask
3): Create a .py file for your app
    >>> New-Item hello.py
4): Setup the Environment Variables for Flask
    >>> $env:FLASK_APP="hello"
    >>> $env:FLASK_ENV="development"
    >>> $env:FLASK_DEBUG=1    